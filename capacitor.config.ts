import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-start-vue-blank',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
